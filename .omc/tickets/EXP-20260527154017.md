# EXP-20260527154017 v3 — QSAR 평가 파이프라인 정직성 강화 (tcrit 반영)

**Status**: EXECUTING (lock active)
**Created**: 2026-05-27 (v1 → v2 cc-converge → v3 tcrit-revised)
**Scope**: data substrate + evaluation honesty + label/generalization probes
**Out-of-scope**: SAE refactor, MMP rule expansion, cluster split, temporal split

## 3-layer 축

```
[목표: 출처를 알고 / 수치를 믿고 / 한계를 본다]
│
├─ 출처를 안다 (data substrate)
│  ├─ T0  (신규)   infra hygiene — weight cache check / artifact naming / GPU preflight policy
│  ├─ T3a (분리)   provenance 스크립트 — 외부에서 이미 작성 (download_chembl_coadd.py 존재). 검증만 남음
│  └─ T3b (분리)   hit-call 규칙 결정 — README/스크립트에 명문화 완료 (max_inhib≥50 OR MIC ≤16 µg/mL OR ≤40k nM). caveat 문서화됨
│
├─ 수치를 믿는다 (evaluation honesty)
│  ├─ T1  scaffold split + fold model 저장 + gap 무조건 보고
│  └─ T4  per-fold SHAP stability — distribution 시각화, threshold 마킹 없음
│
└─ 한계를 본다 (probes — maintainer 책임, 인턴 관찰만)
   ├─ T2  cleanlab audit — depends_on=T3b only, bootstrap CI95
   ├─ T5a 데이터셋 선정 게이트 (2주 timebox)
   └─ T5b T5a PASS 후 외부 validation
```

## 의존성

```
T0 ──┬─► T1 ─► T4
     ├─► T2 (T3b 의존만)
     └─► T5a ─► T5b
T3a/T3b: 이미 외부 완료, T0가 검증만 흡수
```

## GPU 게이트

GPU 필요 task: **T1-unimol-run, T4-unimol-run, T2-run (재학습 시)**.
모든 GPU launch는 다음 게이트 통과해야 함:
1. `runpod-mcp:setup` 또는 `mcp__runpod-mcp__run_preflight` 호출 → `STABLE_OPTIMAL` 또는 `IMPROVING`
2. `plan_gpu_job(randomAccessTrainingGb=...)` 결과의 `containerDiskInGb` 권장값 사용
3. `costSafetyConfirmed`는 사용자 명시 동의시만 true
4. NEVER spot, ALWAYS delete (stop 금지)

ECFP4 path는 GPU 무관 → 게이트 우회 가능.

---

## T0 — infra hygiene [HIGH, 선행]

**Goal**: 후속 모든 티켓이 의존할 공통 인프라.

**Files**:
- `scripts/check_unimol_cache.py` (신규) — HF weight 로컬 캐시 점검, 없으면 RuntimeError
- `tests/test_hit_call.py` (신규) — hit-call 규칙 단위 테스트 (T3b 검증 흡수)
- `docs/EXPECTED_OUTPUTS.md` (업데이트) — artifact naming convention 표 추가
- `requirements.txt` (업데이트) — `cleanlab>=2.6`

**Acceptance**:
- [x] check_unimol_cache.py 실행 시 weight 있으면 PASS 출력, 없으면 RuntimeError + 안내
- [x] test_hit_call.py — 합성 input 5건 (각 active 조건별)으로 rule 통과 검증
- [x] artifact naming 컨벤션 명문화: `{YYYYMMDD}_{split}_{featurizer}.html`

**Effort**: S (~1h)

---

## T3a — provenance 스크립트 [DONE-EXTERNAL, 검증만]

**Status**: `scripts/download_chembl_coadd.py` 외부에서 작성됨 (319 라인). T0의 test_hit_call.py 통과로 검증.

**Acceptance** (검증 only):
- [x] script 존재 + ChEMBL 5 assay 명세 (4 active + 1 empty)
- [x] `data/processed/coadd_pa.csv` 행 수 = 24,120 (확인)
- [x] active rate = 2.86% (확인)

---

## T3b — hit-call 규칙 결정 [DONE-EXTERNAL]

**Status**: 외부에서 명문화됨.
- 규칙: `max_inhibition_pct ≥ 50 OR min_mic_ugmL ≤ 16 OR min_mic_nM ≤ 40_000`
- caveat: censored MIC relation 재검증 안 함 — `data/README.md` + `docs/BACKGROUND.md` §5.4
- 부모 프로젝트 24,120/689 active 정확 재현이 의도된 동작

T0 단위 테스트가 규칙을 코드로 잠금.

---

## T1 — scaffold split evaluation [HIGH, GPU partial]

**Goal**: Bemis-Murcko scaffold 기반 GroupKFold → random vs scaffold AUC 정직 비교.

**Files**:
- `src/qsar_tutorial/data.py` — `scaffold_folds(ds, n_splits=5, seed=42)` 추가
- `src/qsar_tutorial/model.py` — `OOFResult`에 `fold_models`, `fold_indices` 추가 (T4 재사용)
- `examples/run_full_pipeline.py` — `--split {random,scaffold}` flag
- `docs/EXPECTED_OUTPUTS.md` — 두 split AUC 병기

**Acceptance**:
- [x] `--split random` (default), `--split scaffold` CLI flag 동작
- [x] ECFP4 path: random vs scaffold 비교 결과 산출 (GPU 무관, 본 세션)
- [ ] Uni-Mol path: runpod preflight 통과 후 동일 비교 (다음 단계)
- [x] fold별 n_active/n_inactive 기록 → JSON로 보존
- [x] gap (random − scaffold) 무조건 보고 (size 무관, README 섹션 추가)
- [x] N=24,120, fold std 보고

**Effort**: M (~2.5h)

---

## T4 — fold-stability SHAP [MEDIUM, depends T1]

**Goal**: per-fold SHAP top-20 분포 → 불안정 feature 노출 (threshold 마킹 없음).

**Files**:
- `src/qsar_tutorial/shap_layer.py` — `per_fold_top_features()`, `stability_summary()`
- `src/qsar_tutorial/report.py` — stability 섹션 + 표 추가
- `examples/run_full_pipeline.py` — stability 단계 흐름

**Acceptance**:
- [x] HTML 리포트에 fold_presence (0~5) + mean_rank + rank_std 표
- [x] threshold 임의 마킹 **금지**, distribution 그대로 노출
- [x] "5/5 fold = 강한 증거 / 1~2 fold = spurious" 1단락 caveat 자동 삽입
- [ ] random + scaffold 두 split 모두에서 stability (T1 unimol-run 의존)

**Effort**: S-M (~1.5h)

---

## T2 — cleanlab label-noise audit (A/B) [MEDIUM, depends T3b]

**Goal**: T3b 라벨 규칙 명문화된 상태에서 cleanlab 의심 라벨 플래그 → maintainer 수동 검토 → A/B AUPRC + bootstrap CI95.

**Files**:
- `examples/cleanlab_audit.py` (신규)
- `requirements.txt` — `cleanlab>=2.6` (T0)
- `reports/coadd_pa_cleanlab_audit.html` — A/B + 플래그 CHEMBL ID 리스트

**Acceptance**:
- [x] T3b PASS 확인 후 시작 (이미 PASS)
- [x] `find_label_issues(y, oof_proba)` → 플래그 CHEMBL ID CSV
- [ ] 상위 N=20 (또는 전수) **maintainer 수동 검토** (인턴 관찰만) — 본 세션 외 실행 항목
- [x] A (original) vs B (cleaned) AUPRC + top-100 enrichment **bootstrap CI95** (1000 resample)
- [x] paired permutation test p-value
- [x] HTML 리포트 — 결과가 음의 효과여도 그대로 기록

**Effort**: M (~3h)
**Honest risk**: 689 active 극소 표본에서 cleanlab의 circular flag 위험. 부정 결과 가능.

---

## T5a — 외부 데이터셋 선정 게이트 [LOW]

**Goal**: 2주 timebox 안에 다음 게이트 통과 여부 결정. 통과 못 하면 T5b 미개시 + "왜 못 찾았나" 보고서.

**Files**:
- `data/external/README.md` (신규) — 후보 데이터셋 + 게이트 체크리스트

**Acceptance** (게이트):
- [ ] 균주 정합성: P. aeruginosa 직접 OR Gram-negative cross-strain 정당화 문서
- [ ] 라벨 정의: hit-call 임계값이 비교 가능 OR 재라벨링 절차 문서
- [ ] 라이선스: 학술/재배포 가능 명시
- [ ] timebox: 2026-06-10 까지 GO/NO-GO 결정

**Effort**: 본 cycle에선 후보 도출만, 결정은 사용자

---

## T5b — 외부 validation 실행 [BLOCKED by T5a]

**Status**: T5a 통과 시에만 ticket open. 본 cycle scope 외.
